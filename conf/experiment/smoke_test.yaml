# Smoke test overrides - no optimization defaults needed
experiment:
  name: SmokeTest

training:
  total_timesteps: 10000

optimization:
  enabled: false

data:
  regime_definitions:
    train_trending:
      - ["2024-01-01", "2024-01-07"]
